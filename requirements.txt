# Direct dependencies only — transitive packages resolve automatically.
# Regenerate with `pip freeze` only if you need an exact lockfile, and write it
# as UTF-8: `pip freeze | Out-File -Encoding utf8 requirements.txt`

# API
fastapi==0.141.1
uvicorn==0.52.3
pydantic==2.13.4
python-multipart==0.0.32
python-dotenv==1.2.3

# Storage
psycopg2-binary==2.9.12
pgvector==0.5.0

# Ingestion
pypdf==6.16.1

# Trace maths (PCA projection of embeddings for the pipeline visualiser)
numpy==2.5.2

# Models
anthropic==0.122.0
voyageai==0.5.0
